    // Chapter No 06 to 09
    // Task No 01

    // var a = 8;
    // document.write("<h1>Result</h1>");
    // document.write("The Value of a is: " + a + "<br>");
    // document.write("<hr />")

    // document.write("The Value of ++a : " + ++a + "<br>")
    // document.write("Now the value of a is: " + a)
    // document.write("<hr />")

    // document.write("The Value of a++ is: " + a++ + "<br>")
    // document.write("Now the value of a is: " + a)
    // document.write("<hr />")

    // document.write("The Value of --a : " + --a + "<br>")
    // document.write("Now the of a is: " + a)
    // document.write("<hr />")

    // document.write("The Value of a-- is: " + a-- + "<br>")
    // document.write("Now the value of a is : "+ a)

    // // Task 2

    // var a =2, b = 1;
    // document.write("<br> a = " + a + "<br>");
    // document.write("<br> b = " + b + "<br>");

    // document.write("<br> --a  = " + --a + "<br>");
    // document.write("<br> --a - --b = " + (--a - --b) + "<br>");
    // document.write("<br> --a - --b + ++b = "  + (--a - --b + ++b) + "<br>");
    // document.write("<br> --a - --b + ++b + b-- =  " + (--a - --b + ++b + b--) + "<br>");

    // var result = --a - --b + ++b + b--;
    // document.write("<br> result = " + result + "<br>");

    // Task No 03

    // var userName = prompt("Enter Your Name");
    // document.write("Hello " + userName);

    // Task No 05 

    // var userChoice = +prompt("Enter a Table number  your choice")
    // console.log(userChoice);

    // if(userChoice != 0) {
    //     for(var i = 1; i < 11; i++ ) {
    //         document.write(userChoice + " * " + i + " = " + userChoice*i + "<br>");
    //     } 
    // }else 
    //     for(var i = 1; i < 11; i++) {
    //         document.write("5" + " * "+ i + " = " + 5*i + "<br>")
    //     }
    
    // Task No 06 

    // var maths , english , urdu;
    // var totalMarks = 100;
    // maths = 72;
    // english = 78;
    // urdu = 64;
    // var totalMarksAll = totalMarks*3;
    // console.log(totalMarksAll);
    // var total = maths + english + urdu;
    // var totalPer = (total / totalMarksAll) * 100;
    // // console.log(total);
    // var totalMaths = (maths / totalMarks) * 100;
    // var totalEnglish = (english / totalMarks) * 100;
    // var totalUrdu = (urdu / totalMarks) * 100;
    
    // document.write("<table>"
    // +"<th>Subject</th><th>Total Marks</th><th>Marks Obtained</th><th>Percentage</th>" 
    // +"<tr><td>English</td><td><center>"+ totalMarks +" </center></td><td><center>"+ english+"</center></td><td><center>" + totalEnglish +"</center></td></tr>"
    // +"<tr><td>Maths</td><td><center>"+ totalMarks + " </center></td><td><center>"+ maths+"</center></td>><td><center>" + totalMaths +"</center></td></tr>"
    // +"<tr><td>Urdu</td><td><center>"+ totalMarks + "</center></td><td><center>"+ urdu+"</center></td>><td><center>" + totalUrdu +"</center></td></tr>"
    // +"<tr><td></td><td><b><center>"+ totalMarksAll + "</center></b></td><td><b><center>"+ total +"</center></b></td><td><b><center>" + totalPer +"</center></b></td></tr>"
    // + "</table"    
    // );